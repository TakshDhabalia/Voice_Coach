# ui.py

from real_time_inference import real_time_inference

# Example of using the real_time_inference function
if __name__ == "__main__":
    predicted_class = real_time_inference()
    print(f"Predicted class: {predicted_class}")
